// src/components/shuttle-displays/FlightControlsPage.jsx
import React from 'react';
import InopDisplay from './InopDisplay'; // <-- IMPORT the component

const FlightControlsPage = ({ data }) => {
  // When you are ready to build this, you will replace the line below.
  return <InopDisplay title="Flight Control Systems" />;
};

export default FlightControlsPage;