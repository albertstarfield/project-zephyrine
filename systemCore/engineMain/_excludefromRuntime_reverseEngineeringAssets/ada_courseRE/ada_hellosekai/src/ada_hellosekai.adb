-- Adds the Text_IO library to the project
with Ada.Text_IO; use Ada.Text_IO; -- The 'use' clause is a shortcut

-- This is the main program procedure
procedure Ada_Hellosekai is
begin
   -- Now we can call Put_Line directly
   Put_Line ("Hello, Sekai!");
end Ada_Hellosekai;
