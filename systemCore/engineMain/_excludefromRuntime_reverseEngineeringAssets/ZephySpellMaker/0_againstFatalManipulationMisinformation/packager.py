#! /usr/bin/env python3
#Encrypt Zephy Payload Process (On zephy it's need to be handled it special function such as def QuantumDeObfuscatorInjector(): for zephy able to store it to the database and obfuscate it into other text or anonymize it )
#Encode base64 then Encrypts with PostQuantumCryptography pq2 then reencode to base64
#Private Key will be using crafted using time (For this encrypter it will be have menu selection using ncruse do you include the date or just days or days and monts + (with combination The Time like 12:00 or something))
# This is an example code (Accepts the --input or --file then --output into a file with text) Public Key will be using (I'm not sure yet)
