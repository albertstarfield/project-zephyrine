// src/components/shuttle-displays/HorizontalSituationIndicatorPage.jsx
import React from 'react';
import InopDisplay from './InopDisplay'; // <-- IMPORT the component

const HorizontalSituationIndicatorPage = ({ data }) => {
  // When you are ready to build this, you will replace the line below.
  return <InopDisplay title="Horizontal Situation Indicator" />;
};

export default HorizontalSituationIndicatorPage;